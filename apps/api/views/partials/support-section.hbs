<div class="support-section">
    <h2 class="support-title">Need Help?</h2>
    <p class="support-description">
        Our support team is here to help you with any questions or issues regarding your order.
    </p>

    <div class="support-actions">
        <button onclick="alert('Contact Support for Order {{order.id}}')" class="support-btn primary">
            <span class="btn-icon">💬</span>
            <div class="btn-content">
                <div class="btn-title">Contact Support</div>
                <div class="btn-subtitle">Get help from our team</div>
            </div>
        </button>

        <button onclick="alert('Report Issue for Order {{order.id}}')" class="support-btn">
            <span class="btn-icon">⚠️</span>
            <div class="btn-content">
                <div class="btn-title">Report an Issue</div>
                <div class="btn-subtitle">Let us know about a problem</div>
            </div>
        </button>

        {{#if canReturn}}
            <button onclick="alert('Request Return for Order {{order.id}}')" class="support-btn">
                <span class="btn-icon">🔄</span>
                <div class="btn-content">
                    <div class="btn-title">Request Return</div>
                    <div class="btn-subtitle">Start the return process</div>
                </div>
            </button>
        {{/if}}

        {{#if canCancel}}
            <button onclick="if(confirm('Are you sure you want to cancel this order?')) alert('Cancelling Order {{order.id}}')" class="support-btn danger">
                <span class="btn-icon">❌</span>
                <div class="btn-content">
                    <div class="btn-title">Cancel Order</div>
                    <div class="btn-subtitle">Cancel this order</div>
                </div>
            </button>
        {{/if}}
    </div>

    <div class="support-info">
        <div class="info-item">
            <span class="info-icon">📧</span>
            <div class="info-text">
                <strong>Email:</strong> support@ecommerce.com
            </div>
        </div>
        <div class="info-item">
            <span class="info-icon">📞</span>
            <div class="info-text">
                <strong>Phone:</strong> 1-800-SUPPORT
            </div>
        </div>
        <div class="info-item">
            <span class="info-icon">⏰</span>
            <div class="info-text">
                <strong>Hours:</strong> Mon-Fri, 9AM-6PM EST
            </div>
        </div>
    </div>
</div>
