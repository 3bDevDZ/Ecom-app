<div class="payment-info">
    <h2 class="payment-title">Payment Information</h2>

    <div class="info-section">
        <div class="info-header">
            <span class="info-icon">💳</span>
            <h3 class="info-subtitle">Payment Method</h3>
        </div>
        <div class="payment-method">{{order.paymentInfo.method}}</div>
    </div>

    <div class="info-section">
        <div class="info-header">
            <span class="info-icon">🔒</span>
            <h3 class="info-subtitle">Transaction Details</h3>
        </div>
        <div class="transaction-details">
            <div class="transaction-item">
                <span class="transaction-label">Transaction ID:</span>
                <span class="transaction-value">{{order.paymentInfo.transactionId}}</span>
            </div>
            <div class="transaction-item">
                <span class="transaction-label">Payment Date:</span>
                <span class="transaction-value">{{formatDate order.paymentInfo.paidAt}}</span>
            </div>
            <div class="payment-status">
                <span class="status-badge success">✓ Payment Confirmed</span>
            </div>
        </div>
    </div>

    <div class="info-section">
        <div class="info-header">
            <span class="info-icon">📍</span>
            <h3 class="info-subtitle">Billing Address</h3>
        </div>
        <div class="billing-address">
            <div>{{order.paymentInfo.billingAddress.street}}</div>
            <div>{{order.paymentInfo.billingAddress.city}}, {{order.paymentInfo.billingAddress.state}} {{order.paymentInfo.billingAddress.postalCode}}</div>
            <div>{{order.paymentInfo.billingAddress.country}}</div>
        </div>
    </div>
</div>
